# Copy this to .env.local for local dev — never commit .env.local to git

# Lambda Function URL (from AWS Console after deploying lambda/tryon.mjs)
# Vite proxies /api/tryon → this URL during local dev
VITE_TRYON_LAMBDA_URL=https://REPLACE_ME.lambda-url.af-south-1.on.aws
