# AllstoreZA – SAM config
# deploy.sh handles all parameters explicitly — this file is just
# used by SAM for the S3 deployment bucket region default.
version = 0.1

[default.global.parameters]
region = "af-south-1"
